
services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    ports:
      - "8000:5000"   # Host:Container
    environment:
      - FLASK_ENV=production
    restart: unless-stopped